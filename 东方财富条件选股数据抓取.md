# xuangu.eastmoney.com 协议分析报告

## 1. 场景识别

用户执行的是 **东方财富「智能选股 / AI 条件选股」查询结果页操作**，不是注册、登录、支付或 AI 聊天对话。

具体业务场景如下：

1. 用户访问 `xuangu.eastmoney.com` 的选股结果页或编辑页。
2. 页面加载期间进行运营配置、IP 过滤、富文本开关、推荐语等初始化请求。
3. 用户提交或页面自动执行了一条自然语言选股条件：

```text
前一日收盘价涨停，人气排名,非ST；非首板；非科创板；
```

4. 前端调用智能选股接口，将自然语言条件解析为选股查询。
5. 后端返回解析成功，并返回股票列表字段定义及结果数据。
6. 页面随后尝试检查该选股条件是否已收藏。
7. 因用户未登录，收藏状态接口返回：

```json
{
  "code": "304",
  "msg": "请登录以查看更多信息和功能",
  "data": null
}
```

8. 前端未正确捕获该 Promise rejection，触发 Sentry 错误上报。

结论：

> 这是一次未登录状态下的「智能选股条件查询 + 收藏状态检查」流程，核心业务为 AI / 自然语言智能选股。

---

## 2. 交互流程概述

按时间顺序整理如下。

### 2.1 页面初始化阶段

页面首先进行若干初始化检查。

#### 请求 #4

```http
GET https://np-tjxg-operation-b.eastmoney.com/isPreserve?source=WEB
```

用途：

- 查询当前 Web 端是否处于某种运营保护、维护、保留态或功能开关状态。
- 返回：

```json
{
  "code": "100",
  "msg": "success",
  "data": false
}
```

说明当前未启用 preserve 状态。

---

#### 请求 #5

```http
GET https://np-tjxg-operation-b.eastmoney.com/ip/needFilter
```

用途：

- 判断当前 IP 是否需要被过滤、限制或进入降级逻辑。
- 返回：

```json
{
  "code": "100",
  "msg": "success",
  "data": false
}
```

说明当前 IP 不需要过滤。

---

### 2.2 智能选股查询阶段

#### 请求 #8：核心请求

```http
POST https://np-tjxg-b.eastmoney.com/api/smart-tag/stock/v3/pw/search-code
Content-Type: application/json
```

请求体核心字段：

```json
{
  "needAmbiguousSuggest": true,
  "pageSize": 50,
  "pageNo": 1,
  "fingerprint": "4c56b838e26c973d923884420117e292",
  "matchWord": "",
  "shareToGuba": false,
  "timestamp": "1777793119117287",
  "requestId": "idBSnPq0vhqG8T5kvhsvIY9Ix89IOOHS1777793128496",
  "removedConditionIdList": [],
  "ownSelectAll": false,
  "needCorrect": true,
  "client": "WEB",
  "product": "",
  "needShowStockNum": false,
  "biz": "web_ai_select_stocks",
  "xcId": "xc1128ba42de0700ae2a",
  "gids": [],
  "dxInfoNew": [],
  "keyWordNew": "前一日收盘价涨停，人气排名,非ST；非首板；非科创板；",
  "customDataNew": "[{\"type\":\"text\",\"value\":\"前一日收盘价涨停，人气排名,非ST；非首板；非科创板；\",\"extra\":\"\"}]"
}
```

用途：

- 将用户输入的自然语言选股条件提交给智能选股后端。
- 后端进行条件解析、纠错、股票筛选。
- 返回股票列表字段和结果。

响应摘要：

```json
{
  "code": "100",
  "msg": "解析成功，查询到数据",
  "data": {
    "resultType": null,
    "matchable": null,
    "result": {
      "columns": [
        {
          "title": "序号",
          "key": "SERIAL"
        },
        {
          "title": "代码",
          "key": "SECURITY_CODE",
          "indexName": "SECURITY_CODE",
          "dataType": "String"
        },
        {
          "title": "名称",
          "key": "SECURITY_SHORT_NAME",
          "indexName": "SECURITY_SHORT_NAME",
          "dataType": "String"
        },
        {
          "title": "市场简称",
          "key": "MARKET_SHORT_NAME"
        },
        {
          "title": "最新价",
          "key": "NEWEST_PRICE",
          "dateMsg": "2026.04.30",
          "sortable": true
        }
      ]
    }
  }
}
```

业务含义：

- `code=100` 表示请求成功。
- `msg=解析成功，查询到数据` 表示自然语言条件被成功解析。
- `columns` 描述结果表格列。
- 响应体被截断，但可以确认接口返回了选股结果数据结构。

---

### 2.3 再次初始化 / 状态检查

#### 请求 #9

```http
GET https://np-tjxg-operation-b.eastmoney.com/isPreserve?source=WEB
```

与 #4 相同，可能是页面刷新结果区或切换状态后再次检查运营开关。

响应：

```json
{
  "code": "100",
  "msg": "success",
  "data": false
}
```

---

#### 请求 #10

```http
GET https://np-tjxg-operation-b.eastmoney.com/ip/needFilter
```

与 #5 相同，重复检查 IP 过滤状态。

响应：

```json
{
  "code": "100",
  "msg": "success",
  "data": false
}
```

---

### 2.4 收藏状态检查阶段

#### 请求 #12

```http
POST https://np-tjxg-b.eastmoney.com/api/smart-tag/stock/bg/interact/whetherCollect
Content-Type: application/json
```

请求体：

```json
{
  "dynamicType": "COMMON",
  "extraCondition": "",
  "dataNew": "前一日收盘价涨停，人气排名,非ST；非首板；非科创板；",
  "customDataNew": "[{\"type\":\"text\",\"value\":\"前一日收盘价涨停，人气排名,非ST；非首板；非科创板；\",\"extra\":\"\"}]"
}
```

用途：

- 判断当前选股条件是否已经被用户收藏。
- 属于用户交互相关接口。
- 该接口需要登录态。

响应：

```json
{
  "code": "304",
  "msg": "请登录以查看更多信息和功能",
  "data": null
}
```

业务含义：

- 用户当前未登录。
- 后端拒绝返回收藏状态。
- `304` 在此处不是 HTTP 304，而是业务码，表示需要登录。

---

### 2.5 前端错误监控上报

#### 请求 #14

```http
POST https://fenewmonitor.eastmoney.com/api/33/store/?sentry_key=b566dfe4814e4a1b97eedb8a7752bb78&sentry_version=7
Content-Type: text/plain;charset=UTF-8
```

用途：

- Sentry 浏览器 SDK 上报前端异常。

异常信息：

```json
{
  "exception": {
    "values": [
      {
        "type": "UnhandledRejection",
        "value": "Non-Error promise rejection captured with keys: code, data, message, msg"
      }
    ]
  },
  "extra": {
    "__serialized__": {
      "code": "304",
      "msg": "请登录以查看更多信息和功能",
      "data": null,
      "message": "请登录以查看更多信息和功能"
    }
  },
  "level": "error",
  "platform": "javascript"
}
```

说明：

- `whetherCollect` 返回未登录错误。
- 前端 Promise rejection 未被业务代码捕获。
- Sentry 自动采集并上报异常。
- 这不是业务请求失败，而是前端错误监控行为。

---

## 3. API 端点清单

### 3.1 明确出现在请求日志中的端点

| 序号 | 方法 | 域名 | 路径 | 用途 | 是否需要登录 |
|---|---|---|---|---|---|
| #4 | GET | `np-tjxg-operation-b.eastmoney.com` | `/isPreserve?source=WEB` | 查询运营保护 / 功能开关状态 | 否 |
| #5 | GET | `np-tjxg-operation-b.eastmoney.com` | `/ip/needFilter` | 判断当前 IP 是否需要过滤 | 否 |
| #8 | POST | `np-tjxg-b.eastmoney.com` | `/api/smart-tag/stock/v3/pw/search-code` | 智能选股核心查询接口，自然语言条件解析并返回股票结果 | 否，当前未登录也可用 |
| #9 | GET | `np-tjxg-operation-b.eastmoney.com` | `/isPreserve?source=WEB` | 重复运营状态检查 | 否 |
| #10 | GET | `np-tjxg-operation-b.eastmoney.com` | `/ip/needFilter` | 重复 IP 过滤检查 | 否 |
| #12 | POST | `np-tjxg-b.eastmoney.com` | `/api/smart-tag/stock/bg/interact/whetherCollect` | 查询当前条件是否已收藏 | 是 |
| #14 | POST | `fenewmonitor.eastmoney.com` | `/api/33/store/` | Sentry 前端异常监控上报 | 否 |

---

### 3.2 Sentry breadcrumbs 中出现的关联端点

这些请求未完整记录在请求日志中，但出现在 Sentry 面包屑中，说明页面生命周期中确实发生过。

| 方法 | URL | 用途推测 |
|---|---|---|
| GET | `//np-tjxg-operation-b.eastmoney.com/richText/switch?source=WEB&_t=1777793119121` | 查询富文本输入或展示开关 |
| GET | `//np-ipick.eastmoney.com/strategy/recommendSentence?trace=...&count=12&client=web&type=1&X-NPCAT-TRACE-APP-ID=1251` | 获取智能选股推荐语句 |
| POST | `//anonflow2.eastmoney.com/backend/api/webreport` | 匿名埋点 / 行为上报 |
| GET | `//np-tjxg-b.eastmoney.com/api/smart-tag/stock/v3/getXcIdDetail?xcId=xc1128b99e5707003f36` | 根据 `xcId` 获取已有选股条件详情 |

---

## 4. 鉴权机制分析

### 4.1 当前是否存在登录态

从数据看，当前用户 **未登录**。

证据：

1. 鉴权链中显示：

```text
(无鉴权数据)
```

2. 收藏状态接口返回：

```json
{
  "code": "304",
  "msg": "请登录以查看更多信息和功能",
  "data": null
}
```

3. Sentry 上报中的用户信息为空：

```json
"user": {
  "id": "",
  "username": "",
  "userData": {
    "passport": {
      "ct": "",
      "ut": "",
      "uid": "",
      "nickname": ""
    }
  }
}
```

其中：

- `ct` 为空
- `ut` 为空
- `uid` 为空
- `nickname` 为空

说明未携带东方财富 Passport 登录态。

---

### 4.2 请求中携带的 Cookie

请求携带了以下 Cookie：

```text
st_si=00766548207543
st_psi=20260503152518396-119144370786-5655487659
qgqp_b_id=4c56b838e26c973d923884420117e292
st_nvi=Mop-xsGVJYCrVNJuV3nee2e3d
st_pvi=39794301471249
st_sp=2026-05-03%2015%3A25%3A18
st_inirUrl=
st_sn=2
nid18=0140ea2cdc1365b13bf7d37bc984934d
nid18_create_time=1777793119282
gviem=Ve5Qfq9r6j07R0FIy3YdN472d
gviem_create_time=1777793119283
st_asi=20260503152518396-119144370786-5655487659-webznxg.dbssk.qxg-1
```

这些 Cookie 更像是：

| Cookie | 作用推测 |
|---|---|
| `qgqp_b_id` | 浏览器 / 设备指纹 ID，同时作为 `fingerprint` 提交 |
| `st_si` | 会话访问 ID |
| `st_psi` | 页面访问 / 统计 ID |
| `st_nvi` | 新访客标识 |
| `st_pvi` | 持久访客 ID |
| `st_sp` | 首次访问时间 |
| `st_sn` | 当前会话序号 |
| `st_asi` | 当前页面或应用访问链路 ID |
| `nid18` | 匿名设备或风控相关 ID |
| `gviem` | 匿名设备或风控相关 ID |

未观察到典型登录 Cookie，例如 Passport 用户凭据、`uid`、`ut`、`ct` 等有效登录字段。

---

### 4.3 凭据获取流程

本次数据中没有登录流程，因此未观察到：

- 登录页面请求
- 短信验证码请求
- 用户名密码提交
- OAuth / Passport 回调
- 登录 Set-Cookie
- Token 刷新

因此无法从本次数据还原登录凭据获取流程。

---

### 4.4 凭据传递方式

当前所有业务请求主要通过 Cookie 传递匿名身份信息：

```http
Cookie: st_si=...; qgqp_b_id=...; nid18=...; gviem=...
```

核心查询接口还在 JSON body 中显式传递：

```json
{
  "fingerprint": "4c56b838e26c973d923884420117e292"
}
```

其中该值与 Cookie 中的 `qgqp_b_id` 一致：

```text
qgqp_b_id=4c56b838e26c973d923884420117e292
```

说明该站点会将匿名浏览器指纹同时放在：

1. Cookie 中；
2. 智能选股请求 body 的 `fingerprint` 字段中。

---

## 5. 流式通信分析

未检测到 SSE 或 WebSocket。

证据：

- 无 `EventSource` 请求。
- 无 `text/event-stream` 响应。
- 无 `Upgrade: websocket`。
- 无 WebSocket 帧数据。
- 鉴权链和流式通信字段均显示：

```text
(无流式通信)
```

结论：

> 本次智能选股查询是普通 HTTP JSON API 调用，不是流式 AI 对话接口。

---

## 6. 存储使用分析

### 6.1 Cookie 变化

记录中显示 cookies 有 13 项变化：

```text
0: "[object Object]" → "[object Object]"
1: "[object Object]" → "[object Object]"
...
12: "[object Object]" → "[object Object]"
```

但变化内容被序列化为 `[object Object]`，无法直接看到具体字段级变更。

结合请求头可以确认当前请求使用了以下 Cookie：

```text
st_si
st_psi
qgqp_b_id
st_nvi
st_pvi
st_sp
st_inirUrl
st_sn
nid18
nid18_create_time
gviem
gviem_create_time
st_asi
```

这些 Cookie 主要用于：

1. 访问统计；
2. 匿名用户识别；
3. 设备指纹；
4. 风控；
5. 页面链路追踪。

---

### 6.2 localStorage

未提供 localStorage 变化记录。

无法确认是否存在：

- 页面缓存的选股条件；
- 用户偏好；
- 历史搜索；
- 前端路由状态；
- 用户信息缓存。

---

### 6.3 sessionStorage

未提供 sessionStorage 变化记录。

无法确认是否存在一次性页面状态或临时查询数据。

---

### 6.4 登录态存储

本次未发现有效登录态存储。

Sentry 用户信息为空：

```json
"passport": {
  "ct": "",
  "ut": "",
  "uid": "",
  "nickname": ""
}
```

因此可以判断：

> 当前用户以匿名身份访问，只有匿名 Cookie 和指纹，没有有效账号登录凭据。

---

## 7. 关键依赖关系

### 7.1 页面初始化依赖

```text
页面加载
  ├─ /isPreserve?source=WEB
  ├─ /ip/needFilter
  ├─ /richText/switch
  ├─ /strategy/recommendSentence
  ├─ /getXcIdDetail?xcId=...
  └─ /webreport
```

说明：

- `isPreserve` 和 `ip/needFilter` 是通用环境检查。
- `richText/switch` 影响输入框或条件展示能力。
- `recommendSentence` 用于推荐选股语句。
- `getXcIdDetail` 用于根据 URL 中的 `xcId` 恢复已有条件。
- `webreport` 是埋点，不影响主流程。

---

### 7.2 智能选股查询依赖

核心请求：

```text
search-code
```

依赖字段包括：

| 字段 | 来源 / 作用 |
|---|---|
| `fingerprint` | 来自 Cookie `qgqp_b_id` |
| `timestamp` | 前端生成的时间戳 |
| `requestId` | 前端生成的请求唯一 ID |
| `xcId` | 当前选股条件 ID / 页面上下文 ID |
| `keyWordNew` | 用户输入的自然语言条件 |
| `customDataNew` | 条件结构化文本数组 |
| `client` | 固定为 `WEB` |
| `biz` | 固定为 `web_ai_select_stocks` |

---

### 7.3 收藏状态检查依赖

```text
search-code 成功返回 / 页面展示结果
  └─ whetherCollect
```

`whetherCollect` 依赖当前条件文本：

```json
{
  "dataNew": "前一日收盘价涨停，人气排名,非ST；非首板；非科创板；",
  "customDataNew": "[{\"type\":\"text\",\"value\":\"前一日收盘价涨停，人气排名,非ST；非首板；非科创板；\",\"extra\":\"\"}]"
}
```

同时依赖登录态。

由于当前未登录，因此返回业务错误：

```json
{
  "code": "304",
  "msg": "请登录以查看更多信息和功能"
}
```

---

### 7.4 错误上报依赖

```text
whetherCollect 返回 code=304
  └─ 前端 Promise rejection 未捕获
      └─ Sentry 上报到 fenewmonitor.eastmoney.com
```

这是典型的前端错误监控链路。

---

## 8. 复现建议

以下为复现该流程的伪代码逻辑。

### 8.1 初始化 Cookie / 指纹

需要准备匿名 Cookie，特别是：

```text
qgqp_b_id=4c56b838e26c973d923884420117e292
```

该值需要同步放入请求体中的 `fingerprint`。

---

### 8.2 请求头模板

```http
Origin: https://xuangu.eastmoney.com
Referer: https://xuangu.eastmoney.com/
User-Agent: Mozilla/5.0 ...
Content-Type: application/json
Cookie: st_si=...; st_psi=...; qgqp_b_id=...; ...
```

---

### 8.3 伪代码

```python
import time
import random
import requests

session = requests.Session()

cookies = {
    "st_si": "00766548207543",
    "st_psi": "20260503152518396-119144370786-5655487659",
    "qgqp_b_id": "4c56b838e26c973d923884420117e292",
    "st_nvi": "Mop-xsGVJYCrVNJuV3nee2e3d",
    "st_pvi": "39794301471249",
    "st_sp": "2026-05-03%2015%3A25%3A18",
    "st_inirUrl": "",
    "st_sn": "2",
    "nid18": "0140ea2cdc1365b13bf7d37bc984934d",
    "nid18_create_time": "1777793119282",
    "gviem": "Ve5Qfq9r6j07R0FIy3YdN472d",
    "gviem_create_time": "1777793119283",
    "st_asi": "20260503152518396-119144370786-5655487659-webznxg.dbssk.qxg-1"
}

session.cookies.update(cookies)

common_headers = {
    "Origin": "https://xuangu.eastmoney.com",
    "Referer": "https://xuangu.eastmoney.com/",
    "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 "
                  "(KHTML, like Gecko) Chrome/134.0.6998.205 Safari/537.36"
}

# 1. 查询运营状态
r1 = session.get(
    "https://np-tjxg-operation-b.eastmoney.com/isPreserve",
    params={"source": "WEB"},
    headers=common_headers
)
print(r1.json())

# 2. 查询 IP 是否过滤
r2 = session.get(
    "https://np-tjxg-operation-b.eastmoney.com/ip/needFilter",
    headers=common_headers
)
print(r2.json())

# 3. 执行智能选股查询
condition = "前一日收盘价涨停，人气排名,非ST；非首板；非科创板；"

custom_data = (
    '[{"type":"text","value":"前一日收盘价涨停，人气排名,非ST；非首板；非科创板；","extra":""}]'
)

timestamp = str(int(time.time() * 1000000))
request_id = "id" + str(random.randint(10**20, 10**21 - 1))

payload = {
    "needAmbiguousSuggest": True,
    "pageSize": 50,
    "pageNo": 1,
    "fingerprint": cookies["qgqp_b_id"],
    "matchWord": "",
    "shareToGuba": False,
    "timestamp": timestamp,
    "requestId": request_id,
    "removedConditionIdList": [],
    "ownSelectAll": False,
    "needCorrect": True,
    "client": "WEB",
    "product": "",
    "needShowStockNum": False,
    "biz": "web_ai_select_stocks",
    "xcId": "xc1128ba42de0700ae2a",
    "gids": [],
    "dxInfoNew": [],
    "keyWordNew": condition,
    "customDataNew": custom_data
}

headers_json = {
    **common_headers,
    "Content-Type": "application/json"
}

r3 = session.post(
    "https://np-tjxg-b.eastmoney.com/api/smart-tag/stock/v3/pw/search-code",
    json=payload,
    headers=headers_json
)
print(r3.json())

# 4. 检查是否收藏
collect_payload = {
    "dynamicType": "COMMON",
    "extraCondition": "",
    "dataNew": condition,
    "customDataNew": custom_data
}

r4 = session.post(
    "https://np-tjxg-b.eastmoney.com/api/smart-tag/stock/bg/interact/whetherCollect",
    json=collect_payload,
    headers=headers_json
)
print(r4.json())

# 未登录时预期：
# {
#   "code": "304",
#   "msg": "请登录以查看更多信息和功能",
#   "data": None
# }
```

---

## 9. 复现注意事项

1. `fingerprint` 应与 Cookie 中的 `qgqp_b_id` 保持一致。
2. `Origin` 和 `Referer` 需要设置为：

```text
https://xuangu.eastmoney.com
https://xuangu.eastmoney.com/
```

3. `search-code` 接口未观察到强登录要求，匿名状态可请求。
4. `whetherCollect` 接口需要登录，否则返回业务码 `304`。
5. 如果需要复现登录后收藏状态，需要补充东方财富 Passport 登录 Cookie，本次数据中未包含。
6. `timestamp` 和 `requestId` 由前端生成，不需要加密证据；本次未观察到签名、加密或 HMAC。
7. 未检测到 WebSocket/SSE，全部可用普通 HTTP 请求复现。

---

## 10. 总结

本次操作链路可以概括为：

```text
匿名用户进入智能选股页面
  → 页面进行运营状态和 IP 过滤检查
  → 前端提交自然语言选股条件
  → 后端解析条件并返回选股结果
  → 前端检查该条件是否收藏
  → 因未登录返回 code=304
  → 前端未捕获异常
  → Sentry 上报错误
```

核心接口是：

```http
POST /api/smart-tag/stock/v3/pw/search-code
```

核心业务参数是：

```json
{
  "keyWordNew": "前一日收盘价涨停，人气排名,非ST；非首板；非科创板；",
  "biz": "web_ai_select_stocks",
  "client": "WEB",
  "fingerprint": "4c56b838e26c973d923884420117e292"
}
```

当前会话为匿名状态，未发现有效登录凭据。